<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Montserrat', sans-serif;
            box-sizing: border-box;
            background: linear-gradient(to bottom, #c81d25, #000); /* Красный сверху, черный снизу */
            color: #000; /* Черный текст */
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        nav ul {
            display: none;
        }

        .container {
            background: #fff; /* Белый фон */
            position: relative;
            padding: 2rem;
            width: 100%;
            max-width: 400px;
            box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.15); /* Тень для контейнера */
            text-align: center;
            animation: fadeIn 0.5s ease-in-out;
        }

        h1 {
            font-size: 2rem;
            margin-bottom: 1.5rem;
            color: #c81d25; /* Красный заголовок */
        }

        label {
            font-weight: bold;
            margin-bottom: 0.5rem;
            display: block;
            text-align: left;
        }

        input[type="text"],
        input[type="password"] {
            width: 92%;
            padding: 10px 15px;
            border: 1px solid #ddd; /* Серые границы */
            margin-bottom: 1rem;
            font-size: 16px;
            color: #555;
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            outline: none;
            border-color: #c81d25; /* Красная рамка при фокусе */
            box-shadow: 0 0 5px rgba(200, 29, 37, 0.5); /* Красное свечение */
        }

        button {
            width: 100%;
            background: #000; /* Черный фон кнопки */
            color: white; /* Белый текст */
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
        }

        button:hover {
            background: #c81d25; /* Красная кнопка при наведении */
            transform: scale(1.03); /* Легкое увеличение */
        }

        button:active {
            transform: scale(1); /* Возврат размера */
        }

        .options {
            margin-top: 1rem;
            font-size: 14px;
            color: #667;
        }

        .options a {
            color: #c81d25; /* Красная ссылка */
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        .options a:hover {
            color: #000; /* Черный текст при наведении */
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .close-btn {
            position: absolute;
            top: -20px;
            right: -20px;
            width: 40px;
            height: 40px;
            background-color: #c81d25; /* Красный фон кнопки закрытия */
            color: white; /* Белый текст */
            text-align: center;
            line-height: 40px;
            font-size: 24px;
            font-weight: bold;
            text-decoration: none;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .close-btn:hover {
            background-color: #000; /* Черный фон при наведении */
            transform: scale(1.1); /* Увеличение при наведении */
        }

        .close-btn:active {
            transform: scale(1); /* Возврат размера */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>

<body>

<nav>
    <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/products">Menu</a></li>
        <li><a href="/add-product">New Product</a></li>
        <li><a href="/contacts">Contacts</a></li>
        <li><a href="/cart">View Cart</a></li>
        <li><a href="/orders">Orders</a></li>
    </ul>
</nav>

<div class="container">
    <a href="/" class="close-btn">&times;</a>
    <h1>Register</h1>
    <form action="/register" method="POST">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" required>
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required>
        <button type="submit">Register</button>
    </form>

    <div class="options">
        <p>Have an account? <a href="/login">Log in here</a>.</p>
    </div>
</div>
</body>
</html>